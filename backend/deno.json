{
  "tasks": {
    "start": "deno run --allow-net --allow-env --allow-read --watch server.ts",
    "test": "deno test",
    "sync-exercises": "deno run --allow-net=raw.githubusercontent.com --allow-write=data scripts/sync_exercise_dataset.ts"
  },
  "deploy": {
    "org": "enmasantos",
    "app": "vitality-vista",
    "runtime": {
      "type": "dynamic",
      "entrypoint": "./server.ts"
    }
  }
}
