{
  "tasks": {
    "start": "deno run --allow-net --allow-env --allow-read --watch server.ts",
    "migrate": "deno run --allow-net --allow-env --allow-read scripts/migrate.ts",
    "seed": "deno run --allow-net --allow-env --allow-read scripts/seed.ts"
  },
  "deploy": {
    "org": "enmasantos",
    "app": "vitality-vista",
    "runtime": {
      "type": "dynamic",
      "entrypoint": "./server.ts"
    }
  }
}
